services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8085}
    ports:
      - '3000:3000'
    networks:
      - novawave-network

networks:
  novawave-network:
    driver: bridge
